name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions: { contents: read }
defaults: { run: { shell: bash } }

jobs:
  check:
    name: check
    runs-on: ubuntu-latest
    steps:
      - { uses: actions/checkout@v6 }
      - { uses: kjanat/runner@f605cb77aa28decc9d7b3c03e50ae47c0e061570 }
      - { uses: dtolnay/rust-toolchain@stable, with: { components: "clippy, rustfmt" } }
      - run: run fmt-check
      - run: run test-all
      - run: run doc-test
      - run: run clippy-strict
      - { uses: dtolnay/rust-toolchain@nightly }
      - run: rustup run nightly -- run doc-nightly

  roundtrip:
    name: round-trip
    runs-on: ubuntu-latest
    steps:
      - { uses: actions/checkout@v6 }
      - { uses: kjanat/runner@f605cb77aa28decc9d7b3c03e50ae47c0e061570 }
      - { uses: dtolnay/rust-toolchain@stable }
      - { run: run selfcheck, shell: bash }
      - { run: run selfcheck, shell: pwsh }
